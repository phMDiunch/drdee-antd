# 📦 Requirements: Material Management (Quản Lý Vật Tư)

> **📋 STATUS: PENDING** - Giai đoạn 1, Tính năng 3  
> **🔗 Parent**: `015 Inventory - Overview.md`  
> **📄 Implementation**: `src/features/inventory/materials/`  
> **⚠️ Dependencies**: `015.1 System Dictionary.md` (UOM, MATERIAL_TYPE, DEPARTMENT, MATERIAL_CATEGORY)

## 🎯 Mục Tiêu

Quản lý danh mục hàng hóa/vật tư trong kho với phân loại chi tiết phục vụ:

- Tính toán khấu hao
- Phân tích chi phí theo bộ môn/thủ thuật
- Quản lý tồn kho theo nhóm
- Cảnh báo hạn sử dụng

## 📋 Phân Loại Vật Tư (4 Cấp)

```
LOẠI VẬT TƯ (Material Type) - Cấp 1
├─ Vật tư tiêu hao
├─ Vật tư không tiêu hao
├─ Công cụ dụng cụ (CCDC)
└─ Tài sản
    │
    └─► BỘ MÔN (Department) - Cấp 2
        ├─ Nội nha
        ├─ Phục hình
        ├─ Nha chu
        ├─ Nhổ răng
        ├─ Implant
        └─ Chỉnh nha
            │
            └─► NHÓM VẬT TƯ (Category) - Cấp 3
                ├─ Nhóm A
                │   └─► PHÂN NHÓM (Sub-category) - Cấp 4
                │       ├─ Phân nhóm A1
                │       └─ Phân nhóm A2
                └─ Nhóm B
```

**Lý do phân loại chi tiết**:

- Tính khấu hao theo loại vật tư
- Báo cáo chi phí theo bộ môn
- Phân tích hiệu quả sử dụng theo nhóm

## 📋 User Stories

### US-1: Xem Danh Sách Vật Tư

**Là User**, tôi muốn:

- Xem danh sách vật tư với phân trang
- Lọc đa điều kiện: Loại, Bộ môn, Nhóm/Phân nhóm
- Tìm kiếm theo tên, mã
- Tìm theo tags
- Sắp xếp theo tên, mã, tồn kho

### US-2: Tạo Mới Vật Tư

**Là User**, tôi muốn:

- Tạo vật tư mới, hệ thống tự sinh mã (MAT0001, MAT0002...)
- Chọn đầy đủ 4 cấp phân loại
- Chọn đơn vị tính từ danh sách
- Nhập mức tồn kho tối thiểu (để cảnh báo)
- Thêm tags tự do

### US-3: Quản Lý Tags

**Là User**, tôi muốn:

- Gõ tags tự do khi tạo vật tư (VD: "dễ vỡ", "cần lạnh", "cao cấp")
- Hệ thống gợi ý tags đã sử dụng trước đó
- Tìm kiếm vật tư theo tags

## 🎨 UI/UX Requirements

### Màn hình Danh sách

```
┌──────────────────────────────────────────────────────────────────────────┐
│  📦 Quản Lý Vật Tư                                    [+ Thêm Vật Tư Mới] │
├──────────────────────────────────────────────────────────────────────────┤
│  Loại: [Tất cả ▼] Bộ môn: [Tất cả ▼] Nhóm: [Tất cả ▼] 🔍 [Tìm kiếm...]   │
│  Tags: [Select multiple: dễ vỡ, cần lạnh, cao cấp...]                     │
│                                                                            │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ Mã VT   │ Tên Vật Tư        │ ĐVT│ Loại    │ Bộ môn  │ Tồn │ Tags│⚙ │  │
│  │─────────┼───────────────────┼───┼─────────┼─────────┼─────┼────┼─│  │
│  │ MAT0001 │ Implant Nobel A   │ Cái│ Tiêu hao│ Implant │ 50  │🏷  │✎│  │
│  │ MAT0002 │ Găng tay Latex S  │ Hộp│ Tiêu hao│ Chung   │ 200 │    │✎│  │
│  │ MAT0003 │ Composite 3M A3   │ Lọ │ Tiêu hao│ Nội nha │ 10  │🏷  │✎│  │
│  │ MAT0004 │ Máy Xray Portable │ Cái│ Tài sản │ Chung   │ 2   │🏷  │✎│  │
│  └────────────────────────────────────────────────────────────────────┘  │
│                                       [◀] Trang 1 / 10 [▶]               │
└──────────────────────────────────────────────────────────────────────────┘
```

### Drawer Thêm/Sửa Vật Tư

```
┌─────────────────────────────────────────────────────┐
│  ✏️  Thêm Vật Tư                             [×]     │
├─────────────────────────────────────────────────────┤
│  [Tab: Thông tin cơ bản] [Tab: Phân loại] [Tab: ...]│
│                                                      │
│  ━━━ THÔNG TIN CƠ BẢN ━━━                           │
│                                                      │
│  Mã vật tư                                           │
│  [ MAT0005 ]  (Tự động sinh)                         │
│                                                      │
│  Tên vật tư *                                        │
│  [________________________________]                  │
│                                                      │
│  Đơn vị tính *                                       │
│  [Dropdown: Chọn ĐVT ▼]                              │
│    - Cái, Hộp, Vỉ, Chai, Kg...                      │
│                                                      │
│  Mô tả / Quy cách đóng gói                           │
│  [________________________________]                  │
│  [________________________________]                  │
│                                                      │
│  ━━━ PHÂN LOẠI ━━━                                   │
│                                                      │
│  Loại vật tư *                                       │
│  [Dropdown: Chọn loại ▼]                             │
│    - Vật tư tiêu hao                                 │
│    - Vật tư không tiêu hao                           │
│    - Công cụ dụng cụ                                 │
│    - Tài sản                                         │
│                                                      │
│  Bộ môn *                                            │
│  [Dropdown: Chọn bộ môn ▼]                           │
│    - Nội nha, Phục hình, Implant...                  │
│                                                      │
│  Nhóm / Phân nhóm vật tư                             │
│  [TreeSelect: Chọn nhóm ▼]                           │
│    ├─ 📁 Nhóm Implant                                │
│    │   ├─ Trụ Implant                                │
│    │   └─ Abutment                                   │
│    └─ 📁 Nhóm Composite                              │
│                                                      │
│  ━━━ CÀI ĐẶT TỒN KHO ━━━                             │
│                                                      │
│  Mức tồn kho tối thiểu (Cảnh báo)                    │
│  [_____] cái (Optional)                              │
│                                                      │
│  ━━━ TAGS & KHÁC ━━━                                 │
│                                                      │
│  Tags (Nhãn)                                         │
│  [Select mode=tags: dễ vỡ, cần lạnh... + Add new]   │
│                                                      │
│  Hình ảnh                                            │
│  [📷 Upload ảnh]  [Preview thumbnail]                │
│                                                      │
│  ☑ Đang sử dụng                                      │
│                                                      │
│         [Hủy]                    [Lưu]               │
└─────────────────────────────────────────────────────┘
```

## 🔧 Functional Requirements

### FR-1: Tự Động Sinh Mã Vật Tư

**Logic**: Tương tự Supplier

- Prefix: `MAT`
- Format: `MAT0001`, `MAT0002`...

```typescript
async function generateMaterialCode(): Promise<string> {
  const lastMaterial = await prisma.material.findFirst({
    where: { code: { startsWith: "MAT" } },
    orderBy: { code: "desc" },
  });

  if (!lastMaterial) return "MAT0001";

  const lastNumber = parseInt(lastMaterial.code.replace("MAT", ""));
  const nextNumber = lastNumber + 1;
  return `MAT${nextNumber.toString().padStart(4, "0")}`;
}
```

### FR-2: Business Rules

1. **Mã vật tư**: Tự sinh, không cho edit
2. **Tên vật tư**: Bắt buộc, tối thiểu 2 ký tự
3. **ĐVT, Loại, Bộ môn**: Bắt buộc chọn từ SystemDictionary
4. **Nhóm/Phân nhóm**: Optional, có cấu trúc cây (TreeSelect)
5. **Tags**: Array of strings, không giới hạn số lượng
6. **Tồn kho tối thiểu**: Optional, số dương hoặc 0
7. **Soft Delete**: Set `isActive = false`

### FR-3: API Endpoints

```
GET    /api/inventory/materials?page=1&limit=20&search=&typeId=&deptId=&categoryId=&tags[]=
GET    /api/inventory/materials/:id
POST   /api/inventory/materials
PUT    /api/inventory/materials/:id
DELETE /api/inventory/materials/:id
GET    /api/inventory/materials/generate-code
GET    /api/inventory/materials/tags                  # Get all distinct tags
```

### FR-4: Search & Filter

**Search**:

- `name` (ILIKE)
- `code` (ILIKE)
- `description` (ILIKE)

**Filter**:

- `materialTypeId`: Lọc theo loại
- `departmentId`: Lọc theo bộ môn
- `categoryId`: Lọc theo nhóm (bao gồm cả con)
- `tags`: Array - Lọc vật tư có ít nhất một trong các tags
- `isActive`: true/false/undefined

**Sort**:

- `name`, `code`, `createdAt` (ASC/DESC)

## 💾 Data Model (Prisma)

```prisma
model Material {
  id              Int      @id @default(autoincrement())
  code            String   @unique // MAT0001, MAT0002... (Tự sinh)
  name            String   // Tên vật tư
  description     String?  // Mô tả, quy cách đóng gói

  // Foreign keys tới SystemDictionary
  uomId           Int      // Đơn vị tính (UOM)
  uom             SystemDictionary @relation("MaterialUom", fields: [uomId], references: [id])

  materialTypeId  Int      // Loại vật tư (MATERIAL_TYPE)
  materialType    SystemDictionary @relation("MaterialType", fields: [materialTypeId], references: [id])

  departmentId    Int      // Bộ môn (DEPARTMENT)
  department      SystemDictionary @relation("MaterialDepartment", fields: [departmentId], references: [id])

  categoryId      Int?     // Nhóm/Phân nhóm (MATERIAL_CATEGORY) - Optional
  category        SystemDictionary? @relation("MaterialCategory", fields: [categoryId], references: [id])

  // Settings
  minStockLevel   Decimal? @db.Decimal(15, 2) // Mức tồn kho tối thiểu
  imageUrl        String?  // URL hình ảnh
  tags            String[] // Array tags (PostgreSQL string array)

  // Metadata
  isActive        Boolean  @default(true)
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  // Relations (sẽ bổ sung)
  receiptDetails  GoodsReceiptDetail[] // Chi tiết phiếu nhập
  issueDetails    GoodsIssueDetail[]   // Chi tiết phiếu xuất
  stockQuants     StockQuant[]         // Tồn kho
  stockMoves      StockMove[]          // Lịch sử biến động

  @@index([code])
  @@index([name])
  @@index([materialTypeId, departmentId, isActive])
  @@index([tags]) // GIN index for array search in PostgreSQL
}
```

## ✅ Validation (Zod Schema)

```typescript
import { z } from "zod";

export const materialSchema = z.object({
  name: z
    .string()
    .min(2, "Tên vật tư phải có ít nhất 2 ký tự")
    .max(200, "Tên không được quá 200 ký tự"),

  description: z
    .string()
    .max(1000, "Mô tả không được quá 1000 ký tự")
    .optional(),

  uomId: z
    .number({
      required_error: "Vui lòng chọn đơn vị tính",
    })
    .int()
    .positive(),

  materialTypeId: z
    .number({
      required_error: "Vui lòng chọn loại vật tư",
    })
    .int()
    .positive(),

  departmentId: z
    .number({
      required_error: "Vui lòng chọn bộ môn",
    })
    .int()
    .positive(),

  categoryId: z.number().int().positive().optional().nullable(),

  minStockLevel: z
    .number()
    .min(0, "Mức tồn kho tối thiểu không được âm")
    .optional()
    .nullable(),

  imageUrl: z
    .string()
    .url("URL hình ảnh không hợp lệ")
    .optional()
    .or(z.literal("")),

  tags: z
    .array(z.string().max(50))
    .max(20, "Không được quá 20 tags")
    .optional()
    .default([]),

  isActive: z.boolean().default(true),
});

export const updateMaterialSchema = materialSchema.partial();
```

## 🧪 Test Cases

### TC-1: Tạo vật tư đầy đủ thông tin

- **Input**: Valid data với tất cả required fields
- **Expected**: Status 201, trả về material với code tự sinh

### TC-2: Tạo vật tư với tags

- **Input**: tags = ["dễ vỡ", "cao cấp", "cần lạnh"]
- **Expected**: Tags được lưu dạng array trong PostgreSQL

### TC-3: Filter theo tags

- **Input**: GET /api/materials?tags[]=dễ vỡ
- **Expected**: Trả về các vật tư có tag "dễ vỡ"

### TC-4: TreeSelect category

- **Input**: Chọn "Trụ Implant" (là con của "Nhóm Implant")
- **Expected**: categoryId lưu ID của "Trụ Implant"

### TC-5: Get all tags

- **Input**: GET /api/materials/tags
- **Expected**: Trả về array distinct tags đã sử dụng

### TC-6: Validate min stock level

- **Input**: minStockLevel = -10
- **Expected**: Status 400, "Không được âm"

### TC-7: Search multi-field

- **Input**: GET /api/materials?search=implant
- **Expected**: Tìm trong name, code, description

### TC-8: Filter theo bộ môn + loại

- **Input**: GET /api/materials?deptId=1&typeId=2
- **Expected**: Chỉ trả về vật tư thỏa mãn cả 2 điều kiện

## 🎯 Acceptance Criteria

- [ ] Hiển thị danh sách vật tư với phân trang
- [ ] Filter đa điều kiện (Loại, Bộ môn, Nhóm, Tags) hoạt động chính xác
- [ ] Search trong name, code, description
- [ ] TreeSelect hiển thị đúng cấu trúc nhóm/phân nhóm
- [ ] Tags Select cho phép thêm mới và gợi ý tags cũ
- [ ] GET /api/materials/tags trả về danh sách distinct tags
- [ ] Upload hình ảnh và hiển thị preview
- [ ] Tự sinh mã không trùng lặp
- [ ] Soft delete giữ nguyên dữ liệu
- [ ] Vật tư không active không hiển thị trong dropdown (phiếu nhập/xuất)

## 📦 Dữ Liệu Mẫu (Seed Data)

```typescript
const materials = [
  {
    code: 'MAT0001',
    name: 'Implant Nobel Biocare Active 4.3x10mm',
    description: 'Trụ implant Nobel Biocare, đường kính 4.3mm, chiều dài 10mm',
    uomId: <cai-uom-id>,
    materialTypeId: <tieu-hao-type-id>,
    departmentId: <implant-dept-id>,
    categoryId: <tru-implant-cat-id>,
    minStockLevel: 10,
    tags: ['cao cấp', 'nhập khẩu', 'Nobel'],
    isActive: true
  },
  {
    code: 'MAT0002',
    name: 'Găng tay Latex không bột size M',
    description: 'Hộp 100 chiếc, không bột, không mùi',
    uomId: <hop-uom-id>,
    materialTypeId: <tieu-hao-type-id>,
    departmentId: <chung-dept-id>,
    categoryId: null,
    minStockLevel: 50,
    tags: ['tiêu hao', 'chung'],
    isActive: true
  },
  {
    code: 'MAT0003',
    name: 'Composite 3M Filtek Z350 XT màu A3',
    description: 'Syringe 4g, composite nano hybrid',
    uomId: <cai-uom-id>,
    materialTypeId: <tieu-hao-type-id>,
    departmentId: <noi-nha-dept-id>,
    categoryId: <composite-cat-id>,
    minStockLevel: 5,
    tags: ['3M', 'composite', 'nội nha'],
    isActive: true
  }
];
```

## 🔄 Integration Points

### Sử dụng bởi:

- **Phiếu Nhập Kho** (015.4): Chọn vật tư khi tạo dòng chi tiết
- **Phiếu Xuất Kho** (015.5): Chọn vật tư để xuất
- **Báo cáo Tồn Kho** (015.6): Hiển thị theo nhóm/bộ môn
- **Cảnh báo Hạn dùng** (015.8): Filter theo loại vật tư

### Phụ thuộc vào:

- **System Dictionary** (015.1): UOM, MATERIAL_TYPE, DEPARTMENT, MATERIAL_CATEGORY

---

**Priority**: 🔴 Critical  
**Estimated Effort**: 2-3 ngày  
**Dependencies**: 015.1 System Dictionary (UOM, Types, Departments, Categories)
