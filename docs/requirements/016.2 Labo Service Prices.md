# 016.2 Labo Service Prices - Bảng Giá Xưởng

## 📋 Tổng Quan

**Feature**: Quản lý bảng giá dịch vụ labo của từng xưởng răng giả

**Mục đích**:

- Setup giá và bảo hành cho từng loại răng giả của từng xưởng
- Cập nhật giá khi xưởng thay đổi bảng giá
- Làm reference cho LaboOrder (snapshot pricing)

**Route**: `/labo-services`

**Status**: ✅ **IMPLEMENTED** (2025-12-04)

---

## 🎯 User Stories

### US-1: Xem bảng giá tất cả xưởng

**Là** Admin/Manager  
**Tôi muốn** xem bảng giá của tất cả xưởng  
**Để** so sánh giá và quản lý

**Acceptance Criteria**:

- [x] Table với columns: Xưởng, Tên dịch vụ, Nhóm DV, Đơn vị, Giá, Bảo hành, Actions
- [x] Search theo tên xưởng hoặc tên dịch vụ (real-time, filter ở frontend)
- [x] Load tất cả dữ liệu (không phân trang)
- [x] Sort theo giá (thấp → cao, cao → thấp)

### US-2: Thêm dịch vụ cho xưởng

**Là** Admin  
**Tôi muốn** thêm dịch vụ mới vào bảng giá xưởng  
**Để** cập nhật khi xưởng có sản phẩm mới

**Acceptance Criteria**:

- [x] Button "Thêm dịch vụ" mở modal
- [x] Form: Xưởng (_), Loại răng giả (_), Giá (_), Bảo hành (_)
- [x] Xưởng: Dropdown chỉ hiển thị suppliers type = "labo-xuong-rang-gia"
- [x] Loại răng giả: Dropdown từ LaboItem (active only)
- [x] Giá: Number input, format VND
- [x] Bảo hành: Dropdown từ `bao-hanh-labo`
- [x] Validation: Unique (supplierId + laboItemId)
- [x] Success: Refresh table, toast success

### US-3: Cập nhật giá/bảo hành

**Là** Admin  
**Tôi muốn** cập nhật giá hoặc bảo hành khi xưởng thay đổi  
**Để** dữ liệu luôn chính xác

**Acceptance Criteria**:

- [x] Click Edit → Modal với data hiện tại
- [x] Cho phép sửa: Giá, Bảo hành (KHÔNG sửa xưởng/loại răng)
- [x] Update trực tiếp (không version history)
- [x] LaboOrder cũ giữ nguyên giá cũ (đã snapshot)
- [x] Success: Update table, toast

### US-4: Xóa dịch vụ

**Là** Admin  
**Tôi muốn** xóa dịch vụ không còn cung cấp  
**Để** dọn dẹp bảng giá

**Acceptance Criteria**:

- [x] Button Delete với confirmation
- [x] Chỉ xóa nếu: Chưa có LaboOrder nào sử dụng
- [x] Nếu đã dùng: Error "Không thể xóa. Dịch vụ này đã có đơn hàng."
- [x] Success: Xóa khỏi DB, refresh table

### US-5: Import/Export Excel

**Là** Admin  
**Tôi muốn** import/export bảng giá Excel  
**Để** cập nhật hàng loạt hoặc backup

**Acceptance Criteria** (Future):

- [ ] Export: Xuất toàn bộ bảng giá ra Excel
- [ ] Import: Upload Excel theo template để cập nhật giá hàng loạt
- [ ] Validation: Check duplicate, giá hợp lệ
- [ ] Preview trước khi import

---

## 🎨 UI Design

### Table View

```
┌──────────────────────────────────────────────────────────────────────────┐
│ Bảng Giá Dịch Vụ Labo            [🔍 Tìm kiếm...] [+ Thêm dịch vụ]     │
├──────────────────────────────────────────────────────────────────────────┤
│ Xưởng        │ Tên dịch vụ       │ Nhóm DV    │ Giá         │ BH  │ Act │
├──────────────────────────────────────────────────────────────────────────┤
│ Anh Nam Labo │ Răng sứ kim loại  │ Răng sứ KL │ 300.000 ₫  │ 1 năm│[✏️][🗑️]│
│ Anh Nam Labo │ Răng sứ titan     │ Răng sứ KL │ 400.000 ₫  │ 1 năm│[✏️][🗑️]│
│ Anh Nam Labo │ Răng sứ Zirconia  │ Răng toàn sứ│700.000 ₫  │ 5 năm│[✏️][🗑️]│
│ Anh Nam Labo │ Răng sứ Katana    │ Răng toàn sứ│700.000 ₫  │ 5 năm│[✏️][🗑️]│
│ Tung Dentalab│ Răng sứ HT Smile  │ Răng toàn sứ│1.100.000 ₫│10 năm│[✏️][🗑️]│
│ Giang Lab    │ Hàm duy trì Hawley│ Chỉnh nha  │ 170.000 ₫  │ 1 năm│[✏️][🗑️]│
└──────────────────────────────────────────────────────────────────────────┘
│ Hiển thị 25 items                                                        │
└──────────────────────────────────────────────────────────────────────────┘
```

### Create Modal

```
┌───────────────────────────────────────────┐
│ [X] Thêm Dịch Vụ Labo                    │
├───────────────────────────────────────────┤
│                                           │
│ Xưởng labo *                             │
│ ┌─────────────────────────────────────┐  │
│ │ Anh Nam Labo              ▼        │  │
│ └─────────────────────────────────────┘  │
│   (Chỉ hiển thị xưởng răng giả)         │
│                                           │
│ Loại răng giả *                          │
│ ┌─────────────────────────────────────┐  │
│ │ Răng sứ Zirconia          ▼        │  │
│ └─────────────────────────────────────┘  │
│   Nhóm: Răng toàn sứ | Đơn vị: Răng     │
│                                           │
│ Giá (VND) *                              │
│ ┌─────────────────────────────────────┐  │
│ │ 700,000                             │  │
│ └─────────────────────────────────────┘  │
│                                           │
│ Bảo hành *                               │
│ ┌─────────────────────────────────────┐  │
│ │ 5 năm                     ▼        │  │
│ └─────────────────────────────────────┘  │
│   (Dropdown từ MasterData: bao-hanh-labo)│
│   Lưu key string: "5-nam"               │
│   Options: 6 tháng, 1 năm, 2 năm,       │
│            5 năm, 7 năm, 10 năm          │
│                                           │
│          [Hủy]  [Lưu]                    │
└───────────────────────────────────────────┘
```

### Edit Modal

```
┌───────────────────────────────────────────┐
│ [X] Cập Nhật Giá Dịch Vụ                 │
├───────────────────────────────────────────┤
│                                           │
│ Xưởng: Anh Nam Labo                     │
│ Dịch vụ: Răng sứ Zirconia               │
│ Nhóm: Răng toàn sứ | Đơn vị: Răng       │
│                                           │
│ ─────────────────────────────────────    │
│                                           │
│ Giá (VND) *                              │
│ ┌─────────────────────────────────────┐  │
│ │ 750,000          (Cũ: 700,000)     │  │
│ └─────────────────────────────────────┘  │
│                                           │
│ Bảo hành *                               │
│ ┌─────────────────────────────────────┐  │
│ │ 7 năm                     ▼        │  │
│ └─────────────────────────────────────┘  │
│   (Hiện tại: 5 năm)                      │
│                                           │
│ ⚠️ Lưu ý: Giá cũ trong các đơn hàng     │
│   trước đó sẽ không thay đổi            │
│                                           │
│          [Hủy]  [Cập nhật]               │
└───────────────────────────────────────────┘
```

---

## 💾 Data Schema

### Prisma Model

```prisma
model LaboService {
  id String @id @default(uuid())

  // References
  supplierId String
  laboItemId String

  // Pricing
  price Int // Giá 1 đơn vị (VND)

  // Store MasterData.key (String), NOT id (UUID)
  warranty String // e.g., "5-nam" (category='bao-hanh-labo')

  // Metadata
  createdById String
  updatedById String
  createdAt   DateTime @default(now()) @db.Timestamptz
  updatedAt   DateTime @updatedAt @db.Timestamptz

  // Relations
  supplier  Supplier @relation(fields: [supplierId], references: [id], onDelete: Cascade)
  laboItem  LaboItem @relation(fields: [laboItemId], references: [id], onDelete: Restrict)
  createdBy Employee @relation("CreatedLaboServices", fields: [createdById], references: [id])
  updatedBy Employee @relation("UpdatedLaboServices", fields: [updatedById], references: [id])

  // Unique constraint: Mỗi supplier chỉ có 1 giá cho mỗi laboItem
  @@unique([supplierId, laboItemId])
  @@index([supplierId])
  @@index([laboItemId])
}
```

**Changes from specification:**

- Model name: `SupplierLaboPrice` → `LaboService` (business domain naming)
- Price type: `Decimal @db.Decimal(10, 0)` → `Int` (consistent with all other prices in system)
- Relation names updated to match model name

### Response Type

```typescript
LaboServiceResponse {
  id: string

  supplier: {
    id: string
    name: string
  } | null

  laboItem: {
    id: string
    name: string
    serviceGroup: string    // "rang-toan-su" (MasterData key)
    unit: string            // "rang" (MasterData key)
  } | null

  price: number             // Int from DB

  warranty: string          // "5-nam" (MasterData key)

  createdBy: {
    id: string
    fullName: string
  } | null

  updatedBy: {
    id: string
    fullName: string
  } | null

  createdAt: string         // ISO datetime
  updatedAt: string         // ISO datetime
}
```

**Implementation Notes:**

- Frontend displays MasterData labels by looking up keys in constants
- No direct FK to MasterData - stores string keys for flexibility
- Price is `number` (from Int DB field), not Decimal

---

## 🔄 API Specification

### Endpoints

#### GET `/api/v1/labo-services`

**Query**:

```typescript
{
  sortBy?: 'price' | 'name'
  sortOrder?: 'asc' | 'desc'
}
```

**Response**:

```typescript
{
  items: SupplierLaboPriceResponse[]
}
```

**Note**: Load tất cả dữ liệu, search và filter thực hiện ở frontend

#### GET `/api/v1/labo-services/:id`

**Response**: `SupplierLaboPriceResponse`

#### POST `/api/v1/labo-services`

**Body**:

```typescript
{
  supplierId: string;
  laboItemId: string;
  price: number; // Int, VND
  warranty: string; // "5-nam" (MasterData key)
}
```

#### PUT `/api/v1/labo-services/:id`

**Body**:

```typescript
{
  price: number; // Chỉ update giá (Int)
  warranty: string; // Chỉ update bảo hành ("5-nam")
}
```

**Note**: Không cho update `supplierId` và `laboItemId` (xóa và tạo mới thay thế)

#### DELETE `/api/v1/labo-services/:id`

**Business Rule**: Chỉ xóa nếu không có LaboOrder nào reference

---

## 🔐 Permissions

### View

```typescript
"labo-services:view";
```

- Roles: admin only

### Create

```typescript
"labo-services:create";
```

- Roles: admin only

### Update

```typescript
"labo-services:update";
```

- Roles: admin only

### Delete

```typescript
"labo-services:delete";
```

- Roles: admin only

---

## 🔐 Validation Rules

### Create

```typescript
CreateLaboServiceSchema = z.object({
  supplierId: z
    .string()
    .uuid("ID xưởng không hợp lệ")
    .min(1, "Vui lòng chọn xưởng"),

  laboItemId: z
    .string()
    .uuid("ID loại răng giả không hợp lệ")
    .min(1, "Vui lòng chọn loại răng giả"),

  price: z
    .number()
    .positive("Giá phải lớn hơn 0")
    .max(100_000_000, "Giá không hợp lý (tối đa 100 triệu)"),

  warranty: z.string().min(1, "Vui lòng chọn bảo hành"),
});
```

### Update

```typescript
UpdateLaboServiceSchema = z.object({
  id: z.string().uuid("ID không hợp lệ"),

  price: z
    .number()
    .positive("Giá phải lớn hơn 0")
    .max(100_000_000, "Giá không hợp lý (tối đa 100 triệu)"),

  warranty: z.string().min(1, "Vui lòng chọn bảo hành"),
});
```

### Business Rules

1. **Unique pair**: (supplierId, laboItemId) phải unique
2. **Valid supplier**: supplierId phải là supplier type = "labo-xuong-rang-gia"
3. **Active LaboItem**: laboItemId phải là LaboItem chưa bị archive
4. **Valid warranty**: warranty phải là một trong: 6-thang, 1-nam, 2-nam, 5-nam, 7-nam, 10-nam
5. **Cannot delete if used**: Không xóa nếu có LaboOrder reference
6. **Update pricing**: Cập nhật giá không ảnh hưởng orders cũ (đã snapshot)

---

## 🧪 Test Cases

### TC-1: Create Success

```
Given: User có quyền, supplier và laboItem hợp lệ
When: Nhập đầy đủ thông tin hợp lệ
Then:
  - SupplierLaboPrice được tạo
  - Toast success
  - Table refresh
```

### TC-2: Create - Duplicate

```
Given: Đã tồn tại (Anh Nam Labo, Răng sứ Katana)
When: Tạo lại cặp này
Then: Error "Dịch vụ này đã có trong bảng giá xưởng"
```

### TC-3: Update Price

```
Given: SupplierLaboPrice có giá 700,000
When: Update giá thành 750,000
Then:
  - Giá được cập nhật
  - LaboOrder cũ vẫn giữ 700,000 (snapshot)
  - LaboOrder mới sẽ lấy 750,000
```

### TC-4: Delete - Cannot Delete

```
Given: SupplierLaboPrice đã có LaboOrder reference
When: Click Delete
Then: Error "Không thể xóa. Dịch vụ này đã có đơn hàng."
```

### TC-5: Search by Supplier (Frontend)

```
Given: Có 25 items đã load từ 3 xưởng: Anh Nam, Tung, Giang
When: Nhập "Anh Nam" vào search box
Then:
  - Filter frontend chỉ hiển thị prices của Anh Nam Labo
  - Không gọi API
  - Cập nhật số lượng hiển thị
```

### TC-6: Sort by Price (Frontend)

```
Given: 25 prices đã load, từ 170k đến 1.2M
When: Click sort by price ASC
Then:
  - Frontend sort hiển thị từ giá thấp đến cao
  - Không gọi API
```

---

## 💡 Business Logic

### Snapshot Pricing Strategy

**Tại sao snapshot?**

- Xưởng thay đổi giá theo thời gian
- LaboOrder cần lưu giá tại thời điểm tạo
- Báo cáo chính xác chi phí đã phát sinh

**How it works:**

```typescript
// Khi tạo LaboOrder
const supplierPrice = await getSupplierLaboPrice(supplierId, laboItemId);

const order = await createLaboOrder({
  ...data,
  unitPrice: supplierPrice.price, // Snapshot
  warranty: supplierPrice.warranty, // Snapshot: "5-nam"
  warrantyLabel: getWarrantyLabel(supplierPrice.warranty), // Snapshot: "5 năm"
  totalCost: supplierPrice.price * quantity,
});
```

**Result:**

- Update SupplierLaboPrice → không ảnh hưởng orders cũ
- Mỗi order giữ giá tại thời điểm đặt hàng
- Báo cáo chính xác theo thời gian

---

## 📊 Related Queries

### Get Active Prices for Dropdown (LaboOrder create)

```typescript
// Khi tạo LaboOrder, chọn xưởng → load prices
GET /api/v1/labo-services?supplierId={id}&includeInactive=false

// Response: List LaboItem với giá của xưởng đó
[
  { laboItemId, laboItemName, price, warranty },
  ...
]
```

---

## 📋 Implementation Checklist

### Backend

- [x] Prisma model: LaboService (renamed from SupplierLaboPrice)
- [x] Migration: Add table + indexes
- [x] Repository: `labo-service.repo.ts`
- [x] Service: `labo-service.service.ts`
- [x] Mapper: `labo-service/_mappers.ts`
- [x] Server Actions: `labo-service.actions.ts`
- [x] API Routes: `/api/v1/labo-services/*`
- [x] Validation: Zod schemas in `labo-service.schema.ts`

### Frontend

- [x] API client: `api.ts` in labo-services feature
- [x] React Query hooks:
  - [x] `useLaboServices.ts`
  - [x] `useLaboServiceById.ts`
  - [x] `useCreateLaboService.ts`
  - [x] `useUpdateLaboService.ts`
  - [x] `useDeleteLaboService.ts`
- [x] Components:
  - [x] `LaboPriceTable.tsx`
  - [x] `LaboPriceFormModal.tsx` (handles both Create + Edit)
- [x] View: `LaboServicesView.tsx`
- [x] Route: `/labo-services` page
- [x] Master data caching: staleTime: Infinity

### Integration

- [x] Update Supplier model: Add laboServices relation
- [x] Update LaboItem model: Add laboServices relation
- [x] Update Employee model: Add created/updated relations
- [x] Seed warranty master data (bao-hanh-labo category)

---

## 🔗 Related Features

- **016.1 Labo Item Master**: Reference LaboItem
- **015.2 Supplier Management**: Reference Supplier (type = labo)
- **016.3 Labo Orders - Daily View**: Get price snapshot khi tạo order
- **016.4 Labo Reports**: Cost analysis dựa trên pricing

---

**Version**: 2.0  
**Last Updated**: 2025-12-04  
**Status**: ✅ Implemented

**Implementation Notes:**

- Model renamed from `SupplierLaboPrice` to `LaboService` for better business domain alignment
- Price field uses `Int` instead of `Decimal` for consistency with entire system
- All CRUD operations working with admin-only access control
- Frontend uses React Query with infinite staleTime for master data caching
- Supplier dropdown filtered to show only "labo-xuong-rang-gia" type
- LaboItem dropdown shows only active (non-archived) items
